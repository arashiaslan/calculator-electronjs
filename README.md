# 🧮 Electron Calculator

A simple and modern **desktop calculator app** built with [Electron.js](https://www.electronjs.org/).  
Supports basic arithmetic operations with a clean UI — runs on Windows, macOS, and Linux.

---

## 🚀 Features
- Basic arithmetic operations (+, −, ×, ÷)  
- Responsive and minimal UI  
- Cross-platform (Windows, macOS, Linux)  
- Lightweight and fast startup  
- Packaged with `electron-builder`

---

## 📦 Installation

1. **Clone this repository**
   ```bash
   git clone https://github.com/username/electron-calculator.git
   cd electron-calculator
1. **Install dependencies**
   ```bash
   npm install
